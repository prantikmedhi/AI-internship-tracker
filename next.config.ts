import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    turbopack: {
      root: "/Users/prantikpratimmedhi/Projects/notion-mcp-phase1 copy",
    },
  },
};

export default nextConfig;
